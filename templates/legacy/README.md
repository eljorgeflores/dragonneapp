# Plantillas legacy (Fase 3)

HTML movidos aquí porque **no** están referenciados por `TemplateResponse` en el código activo.

- `index.html`, `landing.html`, `dashboard.html`, `register.html`, `package_detail.html`, `pricing.html`

La UI viva usa `marketing.html`, `precios.html`, `app.html`, etc.

Antes de borrar definitivamente: buscar en repo, enlaces externos y historial git. Recuperación: mover de vuelta a `templates/` y referenciar desde una ruta si hiciera falta.
